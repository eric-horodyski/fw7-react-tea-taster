import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fw7-react-tea-taster',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
